-- A script that lists all records of the table `second_table` of the hbtn_0c_0 in MySQL server.
-- Display both score and the name.
-- Order should be by scores.
SELECT score, name FROM second_table ORDER BY score DESC;
